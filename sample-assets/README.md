# Sample Assets

This folder includes sample files for local ingestion tests.

Use `sample-assets/game-assets/*` in the setup upload flow.

The parser classifies assets by filename patterns:
- `board*` -> board image
- `property*` -> property card image
- `chance*` -> Chance card image
- `community*` or `chest*` -> Community Chest card image
